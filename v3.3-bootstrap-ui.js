(function(){'use strict';function remove(){document.getElementById('initialAdminCard')?.remove()}function start(){remove();setInterval(remove,1000)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start);else start();})();
